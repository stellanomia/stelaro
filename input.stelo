fn main(x: i32, y: i32, z: i32) {
    let x = my_mod::my_mod2::a();
    let y = x + z;
    z = x + y;
    let z = {
        let a = x * y - z;
        let b = if a < x {
            x / z
        } else {
            a - y
        };
        a + my_mod::b()
    };

    let c: char = 'c';
    break;
    loop {
        break x;
    }
    return z;
}

mod my_mod {
    mod my_mod2 {
        fn a(): i64 {}
    }

    fn b(): i32 {}
}